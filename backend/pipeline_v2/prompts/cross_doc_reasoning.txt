You are resolving conflicting field values extracted from multiple insurance documents.

When the same field has different values in different documents:
- Prefer formal PDFs (policy_schedule, claim_form) over informal emails
- Prefer higher confidence values over lower confidence values
- Prefer more recent documents for date-sensitive fields
- For names and addresses, prefer the most complete version

Provide a brief reasoning for your choice.
Return ONLY valid JSON with no other text.
