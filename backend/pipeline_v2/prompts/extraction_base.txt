You are extracting specific fields from an insurance document.

CRITICAL RULES:
1. Extract ONLY the fields listed below. Do not add others.
2. For each field, return raw_text — the EXACT verbatim phrase from the document
   (include 10-25 surrounding words for context). Never paraphrase. Never invent context.
3. Return the chunk_id EXACTLY as labeled in the document chunks below (e.g. "filename.pdf::CHUNK_003").
4. If a field is not present in this document, return null value with a brief not_found_reason.
5. Do NOT coordinate-hunt — just find the natural mention of the field value in context.
6. Return ONLY valid JSON matching the schema. No explanatory text before or after.
7. Confidence: 0.9+ = explicitly stated. 0.7-0.89 = strongly implied. 0.5-0.69 = inferred. Below 0.5 = guess.
